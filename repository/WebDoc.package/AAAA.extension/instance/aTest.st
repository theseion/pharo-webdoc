*WebDoc-test
aTest
	" `self foo` is no good. But `SmallInteger` is a very nice class but we can write it as well in Behavior or ClassDescription. But also do: and ifTrue:ifFalse: work.
	`some faulty code should parser as well: .` otherwise this sucks.
	More information can be found on http://www.pharo-project.org."
	self foo.
	self class name.