I am the facade for performing searches. 
I use the sublcasses of WDAbstractSearchGroup to gather all the results and display them in a list.

Instance Variables:
	selectedClass		<Behavior>
	searchGroups		<(Collection of: WDSearchGroup)>
	searchTerm			<String>